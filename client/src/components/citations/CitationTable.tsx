import type { CitationCardProps } from "../types/citationCardProps"

export const CitationTable = ({ citation }: CitationCardProps) => {

    // the idea is that the table would be compressed, csv-style, and that
    // this page would decode that into the # of columns, rows, and the content
    // that would then be displayed

    return (
        <div>
            citation table
        </div>
    )
}