

export const MediaCard = ({ media }) => {

    return (
        <div>
            yep
            <div>"{media.mediaTitle}"</div>
            <div>Released: {media.releaseDate}</div>
        </div>
    )
}